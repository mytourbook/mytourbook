# these settings are merged into the file plugin.properties
# version is also set in net.tourbook.application.ApplicationVersion 
0=17.4.0
1=2017

#this do not work but it can when ALL is correctly configured
#0=${unqualifiedVersion}-${version-suffix}